pandas
requests
yfinance
beautifulsoup4
openai
python-dotenv
pypdf
matplotlib

# --- Optional: install only if using Groq Cloud ---
# groq
# (not required — the openai package already works with Groq's OpenAI-compatible endpoint)

# --- Optional: install only if using Google Gemini natively ---
# google-generativeai
# (not required — the openai package already works with Gemini's OpenAI-compatible endpoint)